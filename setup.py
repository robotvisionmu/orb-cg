from setuptools import setup, find_packages

setup(
    name="orb-cg",
    version="0.0.1",
    description="Slim ConceptGraphs + ORB-SLAM for online open-vocab object mapping",
    packages=find_packages(),
)
